/**
 * Enum to define different button categories.
 * Used to distinguish between types of button actions.
 */
export enum BUTTON_CATEGORIES {
  // Category for buttons that handle integer numbers
  NUMBER = "Number",

  // Category for buttons that handle string values
  CHARACTER = "Character",

  // Category for buttons that handle floating-point numbers
  SPECIAL = "Special",
}
